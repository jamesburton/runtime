---
session: dotnet-runtime AVX-512 intrinsics (VNNI + BF16)
status: in_progress
last_updated: 2026-06-12T20:45:24Z
---

<current_state>
Two in-flight PRs on dotnet/runtime, one local integration branch, and one open API-proposal issue. Active waiting on (a) a secondary reviewer / merge for #128365, (b) Tanner's reply to my pivot-confirmation comment on #129323, (c) re-running tests on the `ushort`-shaped #129326. The dev tree (both PRs merged) builds clean.

Three worktrees:
- `C:\dotnet-runtime`           — branch `feature/avxvnni.v512`, head `85e6adf854c`
- `C:\dotnet-runtime-bf16`      — branch `feature/avx512bf16`,    head `49730090096`
- `C:\dotnet-runtime-dev`       — branch `dev` (pushes to `origin/dev-combined` on the fork because the fork has `dev/*` mirror branches that block a leaf `dev` push), head `bc979bc536f`
</current_state>

<completed_work>

PR #128365 — AvxVnni.V512
- All Tanner-requested changes applied across the full review cycle. Approved by Tanner 2026-06-09 03:39 UTC. 0 unresolved review threads.
- Merge cycle 5 completed (merge with upstream/main, fresh JIT-EE GUID `c8914ccf-5f58-453d-a7f3-88a7eea837f7`).
- All 6 Copilot threads addressed: V512_X64 generator-template explanation (4× rebutted), simdSize concern retracted as designed-and-approved, `where T : unmanaged` fix applied.
- Posted top-level retraction comment 09:30 UTC 2026-06-11 after my A/B question was identified as friction against the approved design.

PR #129326 — Avx512Bf16 (DRAFT)
- Filed as draft 13:33 UTC 2026-06-12 with full cross-link to issue #129323.
- Initial implementation in 80cfeb73938 (option-a grouping, `AVX512_BF16` own ISA with `implication AVX512_BF16 -> AVX512v3` and `AVX10v1 -> AVX512_BF16`).
- Fixed BFloat16 namespace (9ebd167ef05) — `System.Numerics.BFloat16`, not `System`.
- Fixed HARDWARE_INTRINSIC sort-order assertion (0d98674458d) — `ConvertToBFloat16` must precede `MultiplyWideningAndAdd` alphabetically (crossgen2 asserts at hwintrinsic.cpp:1120).
- Pivoted to `Vector*<ushort>` shape per Tanner's review (49730090096) — Tanner's exact words: "we could get bfloat16 support in for significantly cheaper if we exposed the initial signatures as Vector128<ushort>".

Issue #129323 — BF16 API proposal
- Issue filed 13:17 UTC 2026-06-12 with hardware table, three grouping options, alignment to #128365 patterns.
- Tanner replied 14:09 UTC with substantial corrections (hardware-table errors, BF16 scope is large, AVX512v4 likely not worthwhile, ship ushort first).
- I replied accepting all corrections and confirming the pivot to ushort. Awaiting his next round (especially on the open AVX10v1-lie vs AVX512v4 question — he leaned no but left it open).

dev branch
- Merged both feature branches at `6e0fc0c6a73`. Conflicts resolved: jiteeversionguid.h (fresh GUID `72ed5902-dd5e-4804-9d74-4504c0968559`), cpufeatures.h (kept lowercase `Avx512Bmm`, added `Avx512Bf16`), HardwareIntrinsicHelpers.cs.
- BFloat16-namespace fix picked up (23779858eb0).
- Sort-order fix applied (eaf19c560f9).
- ushort pivot applied (bc979bc536f).
- Two clean builds verified: `build.cmd clr+libs -c Debug` (8m49s, then 6m20s incremental). 0 warnings, 0 errors. crossgen2 corelib R2R succeeds.
- Targeting pack assembled via `build.cmd libs+packs -c Debug`: `Avx512Bf16` confirmed in `artifacts/bin/microsoft.netcore.app.ref/ref/net11.0/System.Runtime.Intrinsics.dll` AND `artifacts/packages/Release/Shipping/Microsoft.NETCore.App.Ref.11.0.0-dev.nupkg`. The "build failure" was a benign WiX MSI installer step (exit 9009 — WiX not on box); all ref DLLs and the nupkg were produced before it.
</completed_work>

<remaining_work>

PR #128365
- Waiting on a secondary JIT-team reviewer (@EgorBo / @kg / @dotnet/jit-contrib were CC'd by Tanner 2026-06-09 03:40 UTC) or a maintainer merge.
- No action items on my side unless they re-open something.

PR #129326
- 30 unresolved Copilot threads from two draft-review fires (13:36 + 14:19 UTC 2026-06-12). Quick spot-check showed: one legit (potential duplicate-type emission in generated `CorInfoInstructionSet.cs:2779`), several spurious (impl + PNSE pattern, intrinsic stub self-recursion). Triage properly once Tanner's next-round direction is known — many may auto-resolve under the pivot.
- 1 CI failure on last full check: `Build linux-armel checked CoreCLR_NonPortable`. Possibly infra or possibly a real x86_64-ism in the AVX512 code that doesn't compile on ARM cross-build. Investigate if persistent.
- JIT-side special codegen for `VDPBF16PS` / `VCVTNE2PS2BF16` / `VCVTNEPS2BF16` in `hwintrinsiccodegenxarch.cpp` and `lsraxarch.cpp` — left as explicit TODO in the PR body pending Tanner's grouping-direction confirmation. Cannot be wired meaningfully until the ushort-shape signature path through importing/lowering is sketched. Tanner's next reply is the gate.
- Hand-written tests under `src/tests/JIT/HardwareIntrinsics/X86_Avx/Avx512Bf16/` — also blocked on codegen.

Issue #129323
- Waiting on Tanner. My last comment confirmed: pivot to ushort applied; AVX512v4 vs AVX10v1-lie left for him to call.

dev branch
- Re-run a smoke confirming the targeting-pack consumer flow works end-to-end (write a tiny consumer project that calls `Avx512Bf16.IsSupported` against the dev ref pack on a Zen5 host). Reportedly already done by the user externally — confirmed `IsSupported` returns true on Zen5.
- Not currently planning to open a PR for `dev` — it's local integration only.
</remaining_work>

<decisions_made>

- **Stuck with the option-a grouping** (`AVX512_BF16` as its own ISA with implication chain) for the BF16 wiring instead of folding into v3 or using AVX10v1-lie. Tanner's "not convinced AVX512v4 worthwhile" was directional, not a directive; left open in the issue thread for him to call.
- **Pivoted Vector<BFloat16> → Vector<ushort>** per Tanner's explicit suggestion. Matches the F16C / FP16 codebase precedent (none of them expose `Vector<Half>` either). Method **name** `ConvertToBFloat16` kept — names the operation, not the parameter type.
- **Dropped [RequiresPreviewFeatures] gating** — Tanner clarified that was a VNNI-era hardware-availability hedge.
- **Retracted the V128/V256 A/B question on #128365** rather than re-open Tanner's approved design — user feedback after the question cost ~14hr of review delay.
- **dev pushes to `dev-combined` on the fork** because the fork mirrors upstream's `dev/*` namespace which blocks a leaf `dev` push.
</decisions_made>

<blockers>

- **Tanner direction needed on**: (a) AVX10v1-lie vs AVX512v4 vs leave-as-option-a for BF16 grouping; (b) BF16 codegen path (needed before special-codegen TODO can land).
- **Secondary reviewer needed on #128365**: bottleneck is purely human; nothing actionable on my side.
- **BFloat16-typed overloads** as the long-term target — blocked on the broader BFloat16-SIMD plumbing that Tanner described (same root reason F16C/FP16 don't have `Vector<Half>` overloads).
</blockers>

<context>

PR #128365 is in a stable holding pattern — APPROVED, MERGEABLE, BLOCKED. All triaged CI failures are the Helix-infra family (timeouts, wasm flakes). Background monitoring on a day-shape rhythm: 2hr daytime (05:00-18:00 UTC), 3hr evening (18:00-23:00), final triage ~23:00, 6hr quiet skip overnight. Downgrade triggers: new code-classified CI failure, new merge conflict, new maintainer comment, new unresolved thread.

PR #129326 is much earlier and depends on Tanner's next round. Don't push speculative codegen until he confirms direction. The ushort pivot is the right next step on its own — clean build verified.

The user explicitly emphasized treating Tanner's direction as authoritative; one earlier session created friction by pushing an A/B question on top of his approved design. The right pattern: accept corrections, defend his approved choices against Copilot rebuttals, ask focused questions only when genuinely blocked.

dev exists as a local-only integration tree to let the two in-flight PRs be exercised together. The user verified `Avx512Bf16.IsSupported = true` on Zen5 hardware. Both clean builds done — code is correct at compile + corelib R2R level. Runtime smoke would need hardware-specific tests.
</context>

<next_action>

On resume:

1. Run a fast triage cycle against both PRs and the issue:
   - `gh pr view 128365 --repo dotnet/runtime --json reviewDecision,mergeable,mergeStateStatus,statusCheckRollup`
   - `gh pr view 129326 --repo dotnet/runtime --json reviewDecision,mergeable,mergeStateStatus,statusCheckRollup,headRefOid`
   - `gh issue view 129323 --repo dotnet/runtime --json comments --jq '[.comments[] | select(.createdAt > "2026-06-12T14:30:00Z") | {author: .author.login, body: .body[0:300], createdAt}]'`
2. If Tanner has replied on #129323 with grouping direction: act on it in `feature/avx512bf16` and mirror into `dev`.
3. If #128365 has been merged: acknowledge to user, remove from active watch set.
4. If neither moved: continue the existing day-shape monitoring rhythm.

Key invariants:
- Never push speculative changes on top of Tanner's approved design.
- BF16 element type is **`ushort`** per maintainer guidance; do not regress to `BFloat16` unless Tanner explicitly requests it.
- HARDWARE_INTRINSIC entries within an ISA must be alphabetically sorted (assert at `hwintrinsic.cpp:1120`).
- `BFloat16` lives in **`System.Numerics`**, not `System` (12× CS0234 if you forget).
- crossgen2 corelib R2R must compile clean for the build to succeed.
</next_action>
